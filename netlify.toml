[build]
  base    = "apps/landing"
  command = "npm ci && npm run build"
  publish = "apps/landing/dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback (react-router)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

